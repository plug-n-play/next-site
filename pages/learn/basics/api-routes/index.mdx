import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'API Routes',
  courseId: 'basics',
  lessonId: 'api-routes'
};

In the previous lesson, we learnt how to fetch data for pages. Next.js also enables us to create Nodejs based APIs in the same repository!

In this lesson, we would learn how to create and consume APIs in the same mono-repository.

We will create a simple API which returns a list of famous quotes, and gets rendered on our Next.js application

![](https://user-images.githubusercontent.com/6245927/69990978-ce4c9c80-156c-11ea-8d73-69a745d845aa.png)

Let's begin

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;

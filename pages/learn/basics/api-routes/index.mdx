import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'API Routes',
  courseId: 'basics',
  lessonId: 'api-routes'
};

In this lesson, we would learn how to create and consume an API in the same Next.js app.

before moving forward, learn more about [Fetching Data for Pages](/learn/basics/fetching-data-for-pages), we'll be fetching API routes inside our pages.

We'll create a simple API which returns a list of famous quotes, and gets rendered on our Next.js app. The finished app will look like this:

![](https://user-images.githubusercontent.com/6245927/69990978-ce4c9c80-156c-11ea-8d73-69a745d845aa.png)

Let's get started.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
